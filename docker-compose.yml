services:
  osrm-car:
    build: ./docker/osrm
    container_name: osrm-car
    environment:
      OSRM_PROFILE: car.lua
      OSRM_PORT: "5000"
    ports:
      - "5000:5000"
    volumes:
      - ./storage/osrm:/data
    restart: unless-stopped
